@Library('my-lib') _

node {

	stage('initialisation') {
		log()
	}
	
	stage('login with params') {
		log.info("staiwa")
	}

	stage('login with Map') {
		log.error(name:"anas", this, age: 31)
	}
}

customNode()
